[root@162-240-157-118 conf.d]# cat chronicleexhibits.ae.conf 
# /etc/nginx/conf.d/chronicleexhibits.ae.conf

# 1) Redirect all HTTP requests (www and non-www) to HTTPS non-www
server {
    listen       80;
    listen       [::]:80;
    server_name  www.chronicleexhibits.ae chronicleexhibits.ae;

    # Permanent redirect to non-www HTTPS
    return 301 https://chronicleexhibits.ae$request_uri;
}

# 2) Redirect HTTPS www → HTTPS non-www
server {
    listen       443 ssl http2;
    listen       [::]:443 ssl http2;
    server_name  www.chronicleexhibits.ae;

    ssl_certificate     /etc/letsencrypt/live/chronicleexhibits.ae/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chronicleexhibits.ae/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    return 301 https://chronicleexhibits.ae$request_uri;
}

# 3) Serve HTTPS non-www (Next.js app on port 3008)
server {
    listen       443 ssl http2;
    listen       [::]:443 ssl http2;
    server_name  chronicleexhibits.ae;

    ssl_certificate     /etc/letsencrypt/live/chronicleexhibits.ae/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/chronicleexhibits.ae/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;

    # Let's Encrypt ACME challenge
    location ^~ /.well-known/acme-challenge/ {
        root      /var/www/letsencrypt;
        default_type "text/plain";
        try_files $uri =404;
    }

    # Proxy API routes to Next.js on port 3008
    location /api/ {
        proxy_pass         http://localhost:3008;
        proxy_http_version 1.1;
        proxy_set_header   Host                 $host;
        proxy_set_header   X-Real-IP            $remote_addr;
        proxy_set_header   X-Forwarded-For      $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto    $scheme;
        proxy_set_header   Upgrade              $http_upgrade;
        proxy_set_header   Connection           'upgrade';
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy Next.js static assets
    location /_next/static/ {
        proxy_pass          http://localhost:3008;
        proxy_http_version  1.1;
        proxy_set_header    Host             $host;
        proxy_set_header    Upgrade          $http_upgrade;
        proxy_set_header    Connection       'upgrade';
        proxy_cache_bypass  $http_upgrade;

        expires             30d;
        add_header          Cache-Control "public, max-age=2592000";
        access_log          off;
    }

    # All other requests → Next.js pages
    location / {
        proxy_pass         http://localhost:3008;
        proxy_http_version 1.1;
        proxy_set_header   Host                 $host;
        proxy_set_header   X-Real-IP            $remote_addr;
        proxy_set_header   X-Forwarded-For      $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto    $scheme;
        proxy_set_header   Upgrade              $http_upgrade;
        proxy_set_header   Connection           'upgrade';
        proxy_cache_bypass $http_upgrade;
    }
}